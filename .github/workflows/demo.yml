name: Simple Demo

on: [push]

jobs:
  demo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Show files
        run: ls
     
      - name: show content
        run: cat newfile.txt

        
